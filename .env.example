# OpenRouter API key — used for both story generation (Claude Sonnet) and image generation (DALL-E)
OPENROUTER_API_KEY=sk-or-...

# Secret key to protect the /api/generate endpoint
CRON_SECRET=your-secret-key-here
